#include "main.h"
/**
 * add - Entry point.
 * Description: Adds two numbers.
 * @a: Number to add.
 * @b: Number to add.
 * Return: Result of adding numbers.
 */
int add(int a, int b)
{
	int r;

	r = a + b;
	return (r);
}
